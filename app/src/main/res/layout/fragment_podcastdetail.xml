<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <import type="androidx.lifecycle.MutableLiveData"/>
        <variable
            name="viewModel"
            type="com.caldeirasoft.basicapp.presentation.ui.podcastinfo.PodcastInfoViewModel"/>
        <variable name="title" type="String"/>
        <variable name="artist" type="String"/>
        <variable name="albumArtUri" type="String"/>
        <variable name="displayDescription" type="String"/>
        <variable name="inDatabase" type="Boolean"/>
        <variable
            name="onSubscribe"
            type="android.view.View.OnClickListener"/>
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/coordinatorLayout_root"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/windowBackground">

        <com.caldeirasoft.basicapp.presentation.utils.widget.StateAppBarLayout
            android:id="@+id/appbar_layout_root"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <include layout="@layout/fragment_podcastdetail_header"
                    bind:viewModel="@{viewModel}"
                    bind:onSubscribe="@{onSubscribe}"
                    bind:title="@{title}"
                    bind:artist="@{artist}"
                    bind:albumArtUri="@{albumArtUri}"
                    bind:displayDescription="@{displayDescription}"/>

        </com.caldeirasoft.basicapp.presentation.utils.widget.StateAppBarLayout>

        <!-- placeholder -->
        <FrameLayout
            android:id="@+id/shimmerLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            android:alpha="0">

            <include layout="@layout/shimmer_podcastdetail"/>

        </FrameLayout>

        <!-- list -->
        <com.airbnb.epoxy.EpoxyRecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>